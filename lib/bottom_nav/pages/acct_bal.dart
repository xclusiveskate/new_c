import 'package:flutter/material.dart';

class Accountbalance extends StatelessWidget {
  const Accountbalance({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is Account balance widget"),
    );
  }
}
